# RAG embedding dependencies.
torch==2.13.0
sentence-transformers==5.6.0
chromadb==1.5.9

# Hosted LLM inference for the RAG chatbot.
groq==1.5.0

# Terminal UI polish for scripts/chat.py (optional: falls back to plain text if missing).
rich==15.0.0

# Streamlit web chat frontend (app.py). Reuses the same orchestrator as scripts/chat.py.
streamlit==1.59.2

# Monitoring dashboard data aggregation.
pandas==3.0.3
